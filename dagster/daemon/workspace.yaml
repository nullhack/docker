load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes pipelines.
  - grpc_server:
      host: dagster_pipelines
      port: 4000
      location_name: "main_repository"
  - grpc_server:
      host: dagster_pipelines_example
      port: 4000
      location_name: "EXAMPLE_repository"
